<div class='text-center'>
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span for='img' aria-label='money'>💲</span>
    <span class='dollar-amount'>{{format_amount needed_funding}}</span>
    needed
  </p>
  <p>
    <button class='btn btn-primary'>I'll be deleted eventually!</button>
  </p>
  {{! user sees comments }}
  {{#each message.comments as |comment|}}
    <div>{{comment.text}}</div>
  {{/each}}
</div>

{{! create a form to add a new comment  }}